# RALPH STATE — Team Babbage Prep Backlog

> Updated by every loop iteration. Pick the first `TODO` in the highest-priority section, mark `IN_PROGRESS`, build it, mark `DONE`. Add new items if you spot gaps.

## STATUS LEGEND
- `TODO` — not yet started
- `IN_PROGRESS` — claimed by current iteration
- `DONE` — completed
- `SKIP` — out of scope (with reason)

---

## P0 — MUST HAVE BEFORE SATURDAY 9:30 AM

The team cannot start building without these.

### Team identity & coordination
- [x] **babbage-identity.md** — `DONE` — Built: ~700-word brand brief with origin story (1822→1871→1991→2026), 40-word pitch opener to memorise, 4 taglines mapped to use cases, visual references (public-domain image URLs), and a 15-second "why Babbage?" judge answer.
- [x] **saturday-morning-briefing.md** — `DONE` — Built: 15-min minute-by-minute huddle agenda (09:00-09:15). Includes Teammate 2 assignment branching tree, the 5 demo beats with checkpoint times, the LOCKED decisions table (12 calls), 14.5-hour time budget with 5 phone-alarm checkpoints, cut decisions at each checkpoint, and a tiebreaker rule.
- [x] **team-coordination.md** — `DONE` — Built: full ownership map (18 components, each with owner + reviewer + backup), 5 phone-alarm standup times, comms-channel rules, Git strategy (commit-to-main, no PRs, commit message convention), `docs/DECISIONS.md` log format with examples, pairing policy with explicit exceptions, conflict-resolution flow (5 min cap), anti-patterns list, end-of-task protocol.
- [x] **decisions-locked.md** — `DONE` — Built: 9-section master decisions doc. Models per use case w/ fallbacks, software stack (Flask/Vanilla/SQLite locked), 5-agent architecture w/ hard scope each, demo decisions (Rebecca + Vallance Road + Camden as submission sandbox), bounty priority order, full SCOPE CUTS list, privacy positioning honesty rules, README/pitch locked phrasing, and 3 explicitly-still-open questions tagged to checkpoints.

### Sponsor bounty strategies
- [x] **elevenlabs-prize-strategy.md** — `DONE` — Built: "Civic Watcher" framing for the 1h11min agent, architecture diagram, 3-tier memory strategy (full SQLite log + appended context + tool-based recall), pre-seeded synthetic events plan, ElevenLabs Conv AI vs custom-loop decision (14:00 Sat trigger), session-log JSON format judges read, 7 rehearsed judge questions with expected answers, crash-recovery plan via session_log replay, README placement, and ownership/cut order.
- [x] **nemotron-bounty-strategy.md** — `DONE` — Built: Nemotron Super 120B-A12B on the severity agent (hardest reasoning task). Model-fit table w/ why-not for Ultra/Mini/Llama. Architecture w/ in-pipeline placement. **Comparative evidence plan** (side-by-side Llama vs Nemotron on 3 reports). Three on-screen Nemotron placements during demo. README "Powered by Nemotron" template. Risk recovery (NIM cloud fallback if OOM). Three checkpoint decision points (Sat 11/13/17). Rehearsed 30-sec answer to "why Nemotron over Llama?".

### Setup guides (so the friend doesn't burn 2 hours on tooling)
- [x] **dgx-spark-setup.md** — `DONE` — Built: 5-step hands-on guide for the first 30 min on the Spark (10:00 Sat exit criterion). Access modes (local console vs SSH), basic verification commands, model-pull order with sizes, **5-line smoke test in Python** for both Llama and vision, NVIDIA NIM fallback at build.nvidia.com w/ OpenAI-compatible client snippet, common-failure-modes table, hard "do not touch" list (no apt upgrade, no Spark moves, no CUDA changes).
- [x] **cursor-claude-code-setup.md** — `DONE` — Built: 30-min onboarding for AI-curious quant. Tool install (Cursor recommended), GOOD vs BAD table for what AI excels/fails at, the 4-part prompt anatomy (GOAL/CONSTRAINTS/ACCEPTANCE/CONTEXT) with side-by-side bad-vs-good example, the Saturday Cursor flow (read diff before accepting, paste errors back), starter-prompt pointers to `prompts/build-prompt-*.md`, 7 explicit anti-patterns ("make it production-ready" etc.), hourly 3-line habit (git diff → smoke → commit), when to ask AI vs human.
- [x] **elevenlabs-setup.md** — `DONE` — Built: 6-step setup from zero to working Conv AI agent. Account+API-key, TTS smoke test (6-line Python), Scribe STT smoke test, **the Conv AI bounty agent config** w/ pasteable system prompt + first-message + tool sketch, sandbox dashboard test, Twilio handoff. Failure-modes table, anti-patterns, 14:00 Saturday exit criterion w/ 4 acceptance checks. Flagged: ask ElevenLabs staff at Friday 18:15 partner talk about hackathon credits.
- [x] **twilio-setup.md** — `DONE` — Built: 6-step UK Twilio+ElevenLabs outbound-calling setup. Account (start Friday for verification), UK number purchase w/ verified-caller-ID gotcha for trial, ngrok install + tunnel, ElevenLabs-native Twilio integration (Option A) w/ manual webhook (Option B) fallback, **the Python `trigger_call.py` script** that fires the demo moment, failure-modes table, anti-patterns (no premium number, no real council calls), 15:00 Saturday exit criterion.
- [x] **nebius-setup.md** — `DONE` — Built: Nebius positioning (overflow valve, NOT the demo hot path), where it's used / NOT used w/ privacy-story honesty rules, partner-talk credits ask (Sat 09:00), Studio Inference API only (skip GPU compute and storage), 6-line OpenAI-compatible smoke test, `NebiusProvider` snippet that drops into our LLMProvider abstraction, README template that's honest about actual usage, Friday-evening exit criterion w/ explicit cut rule if not working by Sat 9:30.

### Agent specs (what each agent does + the prompt to build it)
- [x] **intake-agent-spec.md** — `DONE` — Built: full intake-agent spec. Input/output Pydantic CivicIssue shape, model choices table w/ rationale, EXIF GPS extraction w/ device_location fallback w/ map-pin fallback, 7-step 90-min build outline, **self-contained Cursor build prompt** (25 lines, fully constrained, with the 18-category taxonomy embedded), failure-modes table w/ specific recovery, smoke-test snippet, hand-off contract to severity agent (category + lat/lon must never be None).
- [x] **severity-agent-spec.md** — `DONE` — Built: full severity-agent spec (Nemotron bounty agent). 0-10 rubric, 4 RAG sources (STATS19/GIAS/NHS/Census) w/ geometric proximity instead of embeddings, citation format (source+fact+distance_m), Nemotron Super w/ NIM and Llama fallbacks, 7-step 2-hour build outline, **self-contained Cursor build prompt** that constrains JSON output and validates citations against RAG rows, smoke test on Rebecca's coords + a suburban pothole for contrast, hand-off to routing.
- [x] **routing-agent-spec.md** — `DONE` — Built: full routing-agent spec — pure lookup, no LLM, fast. Point-in-polygon borough resolution using London boroughs GeoJSON + Shapely (Nominatim fallback). `data/routing_table.json` with concrete entries for Camden/Tower Hamlets/Hackney/Southwark + `_default`/`_default` catch-all. Decision tree (Open311 → LCS → form → email). Tower-Hamlets-routes-cleanly-but-Camden-actually-receives demo handling per decisions-locked.md. Self-contained Cursor prompt, smoke test on 3 known borough coords, hand-off to submission.
- [x] **submission-agent-spec.md** — `DONE` — Built: full submission-agent spec. Channel-dispatch pattern. **Verbatim Open311 POST payload** w/ Camden's jurisdiction_id=camden.gov.uk endpoint (verified live), Python POST + XML token-regex parser, locked description format including the mandatory "[Babbage Civic Agent — auto-submission on behalf of a resident]" AI-disclosure line, 4-borough endpoint table (Camden + Hackney/Southwark/Islington Pro pattern), demo-mode env var that retargets Tower Hamlets routing to Camden submission, Playwright form fallback explicitly CUT, smtplib email fallback w/ Gmail app-password note, self-contained Cursor prompt asserts never-crash-the-orchestrator, hand-off via shared `processed_issues` table to the Watcher.
- [x] **escalation-agent-spec.md** — `DONE` — Built: the Civic Watcher spec — two-faced agent (long-running watcher + on-demand caller, same process). Trigger condition w/ SLA-by-category table + DEMO_TIME_FACTOR cheat for live demo, locked spoken first-message template with variable interpolation + fallbacks, **watcher_session_log SQLite schema** (this is the bounty artifact), pre-seeded synthetic events plan, Cursor build prompt that mandates crash-resume via session_id replay + NO asyncio + use trigger_call from existing wiring, 5-row failure-modes table, end-to-end smoke test that fires a 30-day-old report and verifies sla_breach event appears within 60s.

### The demo
- [x] **demo-script-3min.md** — `DONE` — Built: minute-by-minute demo script. 5 acts (0:00 hook → 0:20 Rebecca submits → 1:00 time-jump card → 1:15 PHONE CALL → 2:30 leaderboard close). Verbatim 40-word opener + 60-word closer to memorise. The phone-call act has the agent's spoken line, the council-rep's response, the fallback recording instruction ("don't pause for awkwardness"). 60-sec between-judges reset checklist, 6 anti-patterns ("don't read the screen," "don't say hopefully"), and the 5 things we want the judge remembering in order.
- [x] **demo-data-package.md** — `DONE` — Built: 7-asset inventory w/ exact file paths + redundancy rule (three machines incl. USB by 20:30 Sat). Rebecca H. persona locked, photo w/ exiftool EXIF-injection command, voice-note transcript locked w/ Scribe-failure fallback, real Vallance Road coords + LSOA + school names, Camden Open311 sandbox pre-flight w/ "don't be the first call of the day" rule, recorded-call.mp4 as the DEFAULT plan + live call as bonus, deterministic seed_events timeline, dashboard cloning a real Tower Hamlets fly-tip ticket for the drawer visuals, 20:30 Sat pre-flight checklist, cold-start reproduction in <2 minutes.
- [x] **submission-checklist.md** — `DONE` — Built: hour-by-hour Sun 08:00→11:00 timeline w/ 10:45 hard pencils-down rule. 08:30 live-vs-recorded decision. 09:00-10:00 video record block w/ 3-tier upload path (YouTube unlisted > Google Drive > GitHub release). README sections checklist incl. the verbatim Nemotron + ElevenLabs bounty sections + honest disclosures. Final clean-clone smoke test on `/tmp/babbage-final`. Submission portal payload (the 150-250 word project description text written out), bounty-entry flags, "do not touch the repo after 11:00" rule, post-submission failure recovery, awards-ceremony sponsor-thanking order.

---

## P1 — VERY HELPFUL, BUILD IF TIME ALLOWS

### Build prompts (what the team pastes into Cursor)
- [x] **build-prompt-orchestrator.md** — `DONE` — Built: paste-into-Cursor prompt for the sequential orchestrator that wires intake→severity→routing→submission. Acceptance test on Rebecca's demo inputs, never-crash guarantee (each stage's failure logged to stage_errors table but downstream stages still run), DEMO_MODE env-var retarget Tower-Hamlets→Camden, one-line-per-stage timing log, 5 common AI-output corrections to apply, 45-min time budget cap.
- [x] **build-prompt-dashboard.md** — `DONE` — Built: 3-turn Cursor prompt sequence for the dashboard. Turn 1 (30 min) — Flask + Leaflet map with status-coloured markers. Turn 2 (45 min) — borough scorecard endpoint + sortable leaderboard table with adoption chips + BETA badge + gold/silver/bronze. Turn 3 (45 min) — side-drawer modal w/ carousel + comments timeline + ESC close. Stress test, common AI corrections (no React, no npm, no jQuery), graceful degradation rule (Turn 1 alone is demo-able).
- [x] **build-prompt-intake-ui.md** — `DONE` — Built: 2-turn Cursor prompt sequence for Rebecca's intake page (`/app`). Turn 1 (45 min) — camera via getUserMedia w/ facingMode environment, voice via MediaRecorder w/ 30s auto-stop, geolocation w/ status, single "Send to Babbage" button enabled only when ready, multipart POST to /api/intake-submit. Turn 2 (45 min) — processing spinner state, Done state w/ green checkmark + filed-to-council + severity rationale + ETA + "Submit another," **PWA manifest + Apple touch icon** so Add-to-Home-Screen creates a native-feel app. iOS HTTPS-via-ngrok gotcha called out, "test on real iPhone" reminder.
- [x] **build-prompt-scraper.md** — `DONE` — Built: stand-alone scraper Cursor prompt. Two-file output (db.py + scrape.py), 3-sec robots.txt delay + custom User-Agent (non-negotiable), specific BeautifulSoup selectors learned from the previous session, regex date parsing (no datetime attrs on FixMyStreet), idempotent ON CONFLICT, full SQLite schema, scrape_london.py multi-borough runner. "Constraints learned the hard way" section captures the gotchas (no time elements, Enfield Southgate doesn't exist post-2024, City of London Corporation slug). CLI acceptance via SQL row counts. 6 common AI corrections. Hand-off note clarifies photos NOT downloaded here (separate task).
- [x] **build-prompt-rag-corpus.md** — `DONE` — Built: Cursor prompt for the one-shot RAG corpus loader (`src/data/build_rag_corpus.py`). 4 SQLite tables (rag_collisions, rag_schools, rag_hospitals, rag_lsoa) all with name+lat+lon+extra_json shape — NO embeddings, geometric proximity only. Lists data-source URLs (TfL STATS19, GIAS, NHS ODS, ONS Census LSOA), BNG→WGS84 transform code snippet (pyproj) for TfL data, local-cache rule. Acceptance via SQL count assertions ~2000 schools / ~5000 collisions / ~4000 LSOAs + a spot check returning "Osmani Primary School" for Rebecca's coords. 6 common AI corrections, cut order if behind (hospitals first).

### Operations
- [x] **friday-evening-prep.md** — `DONE` — Built: pre-arrival checklist (do at home), then minute-by-minute venue playbook 17:30→21:00 mapped to the official agenda. ElevenLabs 18:15 partner-talk credits play, DGX Spark training note-taking checklist, Teammate-2 sticker-system recruit pass + locked-decisions paper huddle, 19:30-20:30 pre-flight (model pulls running overnight, repo scaffolding, requirements.txt, ngrok), **the live Twilio+ElevenLabs test call by 20:30 as THE Friday-night decision moment** (green light = plan A, fail-by-21:30 = recorded fallback locked, no Saturday consumption), pre-departure exit checklist.
- [x] **saturday-cut-list.md** — `DONE` — Built: ordered 11-item cut list, optimised for max-demo-impact-preserved. Each cut has the replacement (mock_rag.py / hardcoded routing / mocked submission / Llama-not-Nemotron-with-NIM-comparison-screenshot / dashboard-walk-through-not-live-pipeline / first-photo-only / single-update-card / recorded-call / plain-leaderboard / no-watcher-bounty-entry) + impact + README note + trigger checkpoint (13:00 / 17:00 / 19:00 / 20:30 / 08:30 Sun). 7 non-negotiables called out (Rebecca, phone segment, public dashboard, README, video, privacy, opener).
- [x] **fallback-plan.md** — `DONE` — Built: live demo-day recovery playbook. 10-second decision tree, "never apologise, narrate the pivot" rule. 10 named failure modes each w/ symptom + first action + verbatim line to say + acceptance criterion + pre-arm step (phone call fails / Spark crashes / model garbage / Twilio rate-limit / Flask down / Ollama OOM / internet drops / Conv AI silent / laptop dies / judge-question agent-fail). Pre-demo team huddle line at the bottom — "the judge will not remember the failure, they'll remember the recovery."
- [x] **saturday-evening-review.md** — `DONE` — Built: 20:00→21:00 review meeting agenda. 9-row "state of the union" expectation table (7+ green = on track). 60-min agenda — state-of-union 5min, triage reds 10min, end-to-end dry run 15min (THE 15 most important minutes), **20:30-20:45 record backup MP4 HARD CHECKPOINT** (3-tier storage: repo + USB + second laptop + optional YouTube), 20:45 dashboard screenshots for Failure-5 fallback, mirror everything to 2 laptops + USB. What-we-record-tonight vs leave-for-Sunday table (key insight: submission video Sunday because Watcher's session log is richer by then). Cut-decision triggers (severity/watcher/submission), pre-departure protocol (Watcher keeps running overnight), 4 anti-patterns, the closing line "sleep is the last feature."
- [x] **sunday-morning-checklist.md** — `DONE` — Built: 07:00→08:30 morning playbook complementary to submission-checklist. 07:00 home departure, 07:45 venue arrival w/ overnight Discord check, **08:00 ordered health checks** (Watcher first because ElevenLabs bounty continuity, then Spark/Ollama, then end-to-end smoke, then **the live-phone two-call decision moment** at 08:10, then dashboard sanity), 08:20 5-min standup w/ owner-table for 08:30→10:45, 08:25 pre-recording warm-up (tabs, airplane mode, notifications). Polish list w/ 10-min-each rule. "Three failures during 07:00→08:30 that kill the demo" callout. Hand-off to submission-checklist at 08:30.

### Pitch & submission
- [x] **pitch-deck-outline.md** — `DONE` — Built: 11-slide live-pitch deck plan separate from the onboarding deck. Slides 1-5 support each demo beat (cover, live intake mirror, time-jump card, live phone-call view, leaderboard). Slides 6-11 are Q&A backup (Nemotron evidence, ElevenLabs Watcher runtime, FixMyStreet comparison table, "what we built in 14.5 hours," 30/60/90 post-hackathon, thank-you + 2 QR codes). Design language matches babbage-identity.md (navy + orange + Inter + JetBrains Mono). Build window 08:30-10:00 Sun, dual-format export (PDF + .key), 6 anti-patterns (no stock photos, no rival-LLM logos, no >6 words per line).
- [x] **demo-video-script.md** — `DONE` — Built: 4-min ± 15 sec submission video script (6 scenes, hard 5:00 ceiling per official rules). Production setup (QuickTime, 1080p, mic test, b-roll from saved screenshots + recorded MP4, minimal cuts). 6 scenes w/ verbatim voice-over text: hook w/ 6000/28/11yr stats, Rebecca meets the app, agent-at-work w/ Nemotron citations + lower-thirds, time-jump + the recorded call playing at full volume w/ no overlay, accountability layer w/ leaderboard, under-the-hood w/ end card. Post-record protocol (one watch, length check, upload unlisted, README + portal, no re-edits after 10:00). 7 anti-patterns (no bullet-reading, no "hi we are team," no music behind VO, no animated emoji).
- [x] **github-readme-template.md** — `DONE` — Built: paste-into-README.md template w/ {{placeholders}} to fill at 10:00 Sun. Front-loaded: tagline → demo video link + live dashboard link → 200-word elevator pitch w/ killer differentiators. Verbatim Nemotron bounty section + ElevenLabs Watcher bounty section. Architecture ASCII + tech-stack table. **Honest disclosures section** (Camden sandbox, synthetic seed events, DEMO_TIME_FACTOR, Rebecca-is-composite). Run-it-locally w/ env var swap to NIM. Sponsor + team. Placeholder fill-in table for 10:00 Sun. 7 anti-patterns (no emoji explosions, no marketing language, no TODOs, no false-LLM credits). Final test: read first 200 words out loud — does it sell?
- [x] **judge-qa-prep.md** — `DONE` — Built: rehearsed Q&A pack. Tier-1 (rehearse cold) — 5 questions with verbatim 20-30 sec answers + "don't say" + landing line that hooks back to closed-loop/leaderboard/privacy: how-uses-Nemotron, how-uses-ElevenLabs, FixMyStreet-difference, what-built-in-24h, what-with-more-time. Tier-2 (paraphrase) — 5 plausible follow-ups (privacy, non-Open311 councils, FixMyStreet data, AI disclosure, do-not-call). Tier-3 (gotcha) — 5 sharp questions w/ defensive answers (did-we-call-a-real-council, is-voice-synthesised, can-councils-block, what-if-model-returns-garbage, six-other-teams-built-this). Anti-patterns under questioning (no "I think," no "good question"). Escape hatch line for unknown questions.

### Technical references
- [x] **open311-reference.md** — `DONE` — Built: 4-borough endpoint reference (Camden verified live + Hackney/Southwark/Islington high-confidence Pro pattern). Service-code discovery via curl to services.xml, **verbatim demo POST as curl** with exact field-encoded body incl. the AI-disclosure prefix, sample 200 + 422 responses w/ token-extraction note, required-field table, rate-limit guidance (no formal limit, self-throttle to 1/30sec for testing), look-up-submitted-report curl for Watcher resolution detection, FixMyStreet-Pro-is-just-a-shim-onto-Symology-Confirm-Jadu callout for Q&A, never-crash-the-orchestrator failure logging rule.
- [x] **rag-data-shopping-list.md** — `DONE` — Built: 5-file concrete inventory w/ canonical URL + size + format + caching path + target SQLite table per source (STATS19, GIAS schools, NHS, LSOA density, London borough GeoJSON for routing). BNG→WGS84 transform reminder for STATS19+GIAS. Each entry has a one-line bash sanity check (the **Osmani-grep** is the demo-flagship check). Smallest-first download order (~3-5 min total). What-we-DONT-download list (DfT AADF, footfall, CQC). Disk + bandwidth budget. Friday-evening pre-fetch of the small GeoJSON.
- [x] **model-decision-table.md** — `DONE` — Built: per-agent model decision matrix. Headline picks (Llama 3.3 workhorse / Nemotron Super severity-only / Qwen vision / Scribe STT / ElevenLabs Conv AI). 8-row full table w/ speed, memory, why-this-model, 2-tier fallback chain. **Spark memory budget table** (3 loaded scenarios + OOM mitigation via NIM cloud). The LLMProvider abstraction snippet (one env var swap). NIM model-name lookup table. The consolidated Llama-vs-Nemotron decision matrix. **30-sec judge answer locked**. 5 anti-patterns (no loading 3 big models at once, no hardcoding model names, no Nemotron for routing).

---

## P2 — STRETCH (only build after P0+P1 are mostly done)

- [ ] **outreach-drip-plan.md** — `TODO` — schedule of when to send which outreach emails (Sunday evening starting). Path: `docs/hackathon-prep/outreach/outreach-drip-plan.md`
- [ ] **pre-mortem.md** — `TODO` — list of plausible failure modes ranked by likelihood, with the mitigation we already prepared. Path: `docs/hackathon-prep/risks/pre-mortem.md`
- [ ] **decisions-still-open.md** — `TODO` — explicit list of decisions we still need to make Friday evening, with the owner and the latest-by time. Path: `docs/hackathon-prep/decisions-still-open.md`
- [ ] **teammate-2-onboarding.md** — `TODO` — short briefing for the unknown 3rd teammate joining Friday evening. Should work whether they're a designer, frontend, or PM. Path: `docs/hackathon-prep/teammate-2-onboarding.md`
- [ ] **post-hackathon-roadmap.md** — `TODO` — what we say when judges ask "what's next?". 30/60/90 day plan. Path: `docs/hackathon-prep/post-hackathon-roadmap.md`

---

## SCRATCHPAD (notes for future iterations)

Each iteration can append observations here. Things like "noticed we need X" or "the intake spec referenced a thing we should formalize."

- Iter 2 noted: saturday-morning-briefing references `docs/DECISIONS.md` as a decision log. Consider adding a "decisions-log-template.md" to P2 so the team has a ready-to-go file shape (one-line entries). Not adding to backlog yet — `team-coordination.md` should cover the convention.
- Iter 2 noted: the briefing locks `Camden` as the demo-submission borough but Rebecca's story is Tower Hamlets. The demo-data-package.md will need to handle this gap — either rewrite Rebecca to a Camden street, or stage the call to Tower Hamlets and the Open311 submission to Camden separately. Flag for that task. → Resolved in iter 4 decisions-locked.md: demo says Tower Hamlets, actual API call goes to Camden, README discloses honestly.
- Iter 5 noted: the ElevenLabs strategy depends on `seed_events.py` and `watcher.py` existing. These should appear in the build-prompt artifacts (P1) so Teammate 2 / Ongun have copy-pasteable prompts. Consider adding `build-prompt-watcher.md` to P1 — *deferred for now, escalation-agent-spec.md should cover it under P0 first.*

---

## STATS

- P0: 19 total · 19 DONE · 0 TODO ✅
- P1: 17 total · 17 DONE · 0 TODO ✅
- P2: 5 total · 0 DONE · 5 TODO

Last update: iteration 36 — model-decision-table.md done · **ALL P0 AND P1 COMPLETE**
